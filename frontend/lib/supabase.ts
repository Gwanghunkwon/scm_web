import { MaterialRow } from "./types";

// Placeholder integration surface for future Supabase migration.
export async function getMaterials(): Promise<MaterialRow[]> {
  return [];
}

export async function getBOM(): Promise<unknown[]> {
  return [];
}

export async function calculateShortage(): Promise<MaterialRow[]> {
  return [];
}
