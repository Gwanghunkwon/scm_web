{
  "framework": "nextjs",
  "installCommand": "sh -c 'if [ -d frontend ]; then cd frontend && npm install; else npm install; fi'",
  "buildCommand": "sh -c 'if [ -d frontend ]; then cd frontend && npm run build && cd .. && rm -rf .next && mv frontend/.next .next; else npm run build; fi'",
  "outputDirectory": ".next"
}
